import actions from './actions.json';
import common from './common.json';
import dashboard from './dashboard.json';
import errors from './errors.json';

const en = { errors, actions, dashboard, common };

export default en;
